import React from "react"
import PhotoGrid from "../images/photo-grid.png"

export default function Hero() {
    return (
        <div>
            <img src={PhotoGrid} alt="grid of pictures" />
        </div>
    )
}